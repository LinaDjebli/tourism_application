import 'dart:ffi';

import 'package:flutter/material.dart';

class Primary_button extends StatelessWidget {
  const Primary_button({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
        // height: height ?? 57,
        //width: width ?? defaultWidth ,
        //child: MaterialButton(
        // disabledColor:Color(Colors.black),
        //onPressed:  isLoading.value || loading || onPressed == null ? null :()=> _handleButtonPressed(isloading ,isMounted )),
        );
  }
}
